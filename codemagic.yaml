workflows:
  ios-workflow:
    name: iOS Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    scripts:
      - name: Build
        script: |
          # Codemagic سيكتشف المشروع تلقائياً
    artifacts:
      - build/ios/ipa/*.ipa
