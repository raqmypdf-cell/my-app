{
  "name": "my-capacitor-app",
  "version": "1.0.0",
  "description": "iOS App via Capacitor",
  "scripts": {
    "build": "echo 'No build step required for static web app'"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
